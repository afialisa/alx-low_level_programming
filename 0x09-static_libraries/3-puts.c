#include "main.h"
#include <stdio.h>
/**
 * _puts - Prints a string which is followed by a new line.
 * @str:  string to be outputed
 */
void _puts(char *str)
{
puts(str);
}

/**
 * main - Entry point of the funtion.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
_puts("\"At the end of the day, my goal was to be the best hacker");
return (0);
}
