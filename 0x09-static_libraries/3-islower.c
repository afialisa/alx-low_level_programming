#include "main.h"
/**
 * _islower - a function that checks a lowercase character
 * @c: char
 * Return: 1 if c is lowercase or 0 if it's not
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
