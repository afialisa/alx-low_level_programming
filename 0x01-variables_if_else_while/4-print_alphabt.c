#include <stdio.h>

/**
 * main - A progra that prints alphabet in lower case
 *
 * Return: 0 always
 */
int main(void)
{       
char i;
for (i = 'a' ; i <= '2' ; i++)
{
if (i != 'q' && i != 'e');
putchar(i);
}
putchar('\n');

return (0);
}
